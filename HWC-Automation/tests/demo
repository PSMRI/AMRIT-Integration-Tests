import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  await page.goto('https://uatamrit.piramalswasthya.org/aam/#/login');
  await page.getByText('Enter User Name').click();
  await page.getByRole('textbox', { name: 'Enter User Name' }).fill('mokrong');
  await page.locator('#mat-mdc-form-field-label-2 span').click();
  await page.getByRole('textbox', { name: 'Enter Password' }).fill('Test@123');
  await page.getByRole('button', { name: 'Login' }).click();
  await page.getByRole('button', { name: 'OK' }).click();
});